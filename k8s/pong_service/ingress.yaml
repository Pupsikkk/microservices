apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pong-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - http:
        paths:
          - path: /api/pong
            pathType: "Prefix"
            backend:
              service:
                name: pong-service
                port:
                  number: 3001